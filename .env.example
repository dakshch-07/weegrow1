DB_HOST=localhost
DB_NAME=webgrowth_db
DB_USER=root
DB_PASS=""

# Encryption Salt for GDPR Hashing
IP_SALT="your_secure_ip_salt_here"

# SMTP Mail Server Config (Leave empty to fallback to PHP mail())
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_ENCRYPTION=tls
SMTP_FROM="no-reply@weegrow.in"
SMTP_FROM_NAME="WeeGROW Leads"
